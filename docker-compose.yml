version: '3.9'

services:
  mammamia:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      TMDB_KEY: ${TMDB_KEY}
    volumes:
      - .:/app
